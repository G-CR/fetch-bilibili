# 项目 TODO（当前版本）

最后更新时间：2026-04-23（当前以运维效率优化和质量专项为主）

说明：

- 本文档只维护当前执行优先级、活动任务与远期讨论，不再保留逐项历史流水账。
- 更细的设计、实现拆分与验收记录，请看 `docs/superpowers/specs/` 与
  `docs/superpowers/plans/`。
- 状态说明：
  - `todo`：未开始
  - `doing`：进行中
  - `done`：已完成
  - `blocked`：有阻塞

## 当前阶段判断

当前项目已经进入：

- 单平台（B 站）单机可运维阶段
- 后端服务 + 独立前端控制台的完整闭环阶段
- 以稳定运行、恢复能力、人工审核和本地存储管理为核心目标的阶段

当前不应再把系统视为“概念验证”或“仅后端原型”。

## 当前执行队列

## P1

- [ ] `todo` 候选池审核与运维效率优化
  - 目标：在保持单平台边界不变的前提下，提高候选池审核效率、评分解释质量和
    运维可见性。
  - 当前方向：
    - 候选来源与评分解释
    - 前端审核操作反馈
    - 任务失败原因和状态展示清晰度

- [ ] `todo` 存储与浏览链路可解释性优化
  - 目标：继续稳固 `store/` 与 `library/` 双层结构，让 cleanup 结果、存储统
    计和浏览投影更容易理解与排查。
  - 当前方向：
    - cleanup 结果解释
    - 浏览投影对异常退出、事件丢失、人工误删的修复能力
    - 存储统计、稀有视频统计和清理结果一致性

## P2

- [ ] `doing` 单平台质量专项
  - 目标：持续补齐当前能力边界内的回归测试、E2E、部署脚本 smoke 与验收闭
    环。
  - 当前方向：
    - 命中路径补测
    - 覆盖率基线维护
    - Shell / PowerShell 部署脚本一致性复验
  - 2026-04-23 已取得的验证证据：
    - `cd frontend && npm run test:vite-config`：通过
    - `cd frontend && npm run test:state`：通过
    - `cd frontend && npm run test:smoke`：通过
    - `cd frontend && npm run build`：通过
    - `cd frontend && npm run test:e2e`（mock 模式）：11 个用例全部通过
    - `docker compose config`：通过
    - `bash scripts/tests/test-deploy.sh`：通过
    - `go test ./... -count=1`：通过（沙箱外复跑）
    - `go test ./... -cover`：通过（沙箱外复跑）
    - `go test ./... -coverprofile=/tmp/fetch-bilibili-cover.out`：
      总覆盖率 `79.8%`
  - 当前阻塞：
    - `pwsh -NoProfile -File scripts/tests/test-deploy.ps1`：当前环境缺少
      `pwsh`，未执行
    - 当前总覆盖率 `79.8%`，低于 `docs/dev-standards.md` 中的 `85%` 基线，
      仍需补测或提高覆盖率

## 已完成里程碑（摘要）

- [x] `done` 单平台稳定性与异常场景验收收口（文档与契约第一轮）
  - 已完成配置写回与重启恢复、启动恢复与任务幂等、`store/` / `library/`
    一致性、B 站认证与风控运行态、API / SSE 契约边界的文档核对
  - 当前未新增实现改造；自动化回归、覆盖率和部署 smoke 复验继续归入 P2
    “单平台质量专项”
- [x] `done` 主入口事实文档与验收入口第一轮收口
  - 已完成 README、API、配置、运行手册、容器部署、数据模型、worker /
    scheduler、开发细则与 TODO 入口的当前实现对齐
  - 后续若再发现偏差，按 review 增量修正，不再作为独立长期挂起任务保留
- [x] `done` cleanup 清理链路与清理后数据一致性
- [x] `done` 任务幂等、重复任务保护与启动恢复
- [x] `done` 候选池自动发现、人工审核与转正闭环
- [x] `done` 运维接口补全与前端驾驶舱首轮闭环
- [x] `done` MySQL 迁移自动化与启动自动迁移
- [x] `done` Cookie / 风控运行态观测增强
- [x] `done` SSE 实时驾驶舱、断线重连与低频对账
- [x] `done` `store/` / `library/` 双层存储投影
- [x] `done` 浏览器级 E2E 与覆盖率补强专项

## 远期讨论（不进入当前执行顺序）

以下方向当前不进入主执行顺序，只保留为远期讨论：

- 多平台扩展（抖音、快手、小红书等）
- App 化
- 多用户 / 权限 / 租户隔离
- 云端对象存储、分布式队列或多节点 worker 集群

## 当前推荐顺序

若没有更高优先级的新需求插入，建议按以下顺序推进：

1. 先处理候选池审核与运维效率优化。
2. 再处理存储与浏览链路可解释性优化。
3. 最后持续补齐单平台质量专项、补测和部署脚本一致性复验。
