# 重复任务保护实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在不引入新表结构的前提下，优先解决任务队列中的高频重复入队问题，避免调度器、手动触发和派生下载任务在 `queued/running` 状态下重复堆积。

**架构：** 将去重保护放在任务入队侧：`fetch/check/cleanup` 按任务类型判重，`download` 按 `video_id` 判重。MySQL 仓储在插入前检查是否已有活动任务；业务层识别重复错误后按“幂等成功”处理，不再上抛失败。

**技术栈：** Go、MySQL、sqlmock、单元测试

---

### 任务 1：补齐失败测试

**文件：**
- 修改：`internal/repo/mysql/job_repo_test.go`
- 创建：`internal/repo/mysql/job_repo_dedup_test.go`
- 修改：`internal/jobs/service_test.go`
- 修改：`internal/worker/handler_test.go`

- [x] **步骤 1：先写失败测试，覆盖普通任务与下载任务的重复入队拦截**
- [x] **步骤 2：先写失败测试，覆盖业务层对重复任务错误的忽略语义**

### 任务 2：实现入队去重

**文件：**
- 修改：`internal/jobs/types.go`
- 修改：`internal/jobs/service.go`
- 修改：`internal/repo/mysql/job_repo.go`
- 修改：`internal/worker/handler.go`

- [x] **步骤 1：新增重复任务哨兵错误**
- [x] **步骤 2：实现 MySQL 入队前活动任务检查**
- [x] **步骤 3：让 jobs service / worker handler 将重复入队视为幂等成功**

### 任务 3：验证与文档同步

**文件：**
- 修改：`docs/todo.md`
- 修改：`docs/superpowers/plans/2026-04-13-job-dedup.md`

- [x] **步骤 1：运行受影响模块测试**
- [x] **步骤 2：运行 `go test ./... -count=1`**
- [x] **步骤 3：更新动态 TODO，标记这一轮完成情况**

## 当前结果

- 本轮已完成“重复任务保护”子任务。
- 后续“清理后的数据一致性”子任务已在独立计划中完成。
- 当前下一优先级已切换为“补全运维接口”。
